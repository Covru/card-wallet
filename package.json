{
  "name": "wallet-local-cards",
  "version": "1.0.0",
  "private": true,
  "description": "A private, offline wallet for membership and loyalty cards. All data stays on the device.",
  "scripts": {
    "assets": "capacitor-assets generate --android",
    "sync": "cap sync android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
